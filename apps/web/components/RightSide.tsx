import React from "react";

const RightSide = ({ width }: { width: number }) => {
  return (
    <div style={{ height: "100%", width: `${width}%`, border: "1px solid #090909" }}>
      
    </div>
  );
};

export default RightSide;